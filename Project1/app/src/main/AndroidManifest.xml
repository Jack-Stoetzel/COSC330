<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project01">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"

        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:label="@string/main" android:name=".MenuList"/>
        <activity android:label="@string/gloss" android:name=".WordList"/>
        <activity android:label="@string/gloss" android:name=".WordDisplay"/>
        <activity android:label="@string/iCrossWord" android:name=".CrosswordActivity"/>
        <activity android:label="@string/iWordSearch" android:name=".WordSearchActivity"/>
        <activity android:label="@string/iBodyPic" android:name=".BodyPic"/>
        <activity android:label="@string/iHeadPic" android:name=".HeadPic"/>
        <activity android:label="@string/iLabelPic" android:name=".LabelPic"/>
        <activity android:label="@string/speak" android:name=".SpeakingExercises"/>
        <activity android:label="@string/speak" android:name=".SpeakingDisplay"/>
        <activity android:label="@string/pic" android:name=".StoryList"/>
        <activity android:label="@string/pic" android:name=".StoryDisplay"/>
        <activity android:label="@string/login" android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
    <!--
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    -->